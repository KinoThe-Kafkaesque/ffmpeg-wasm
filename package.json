{
  "name": "ffmpeg-wasm",
  "private": true,
  "scripts": {
    "ci:browser": "playwright test",
    "test:browser": "playwright test",
    "test:browser:chromium": "playwright test --project=chromium --project=chrome --project=mobile-chromium",
    "test:browser:install": "playwright install chromium firefox webkit"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
